<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="Gallery.Controls.HorizontalCard"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <VerticalStackLayout
        x:Name="Card"
        Padding="20"
        BackgroundColor="Black">
        <Label
            x:Name="CardTitle"
            FontSize="36"
            Text="Título"
            TextColor="White" />

        <Label
            x:Name="CardDescription"
            FontSize="18"
            Text="Texto descritivo"
            TextColor="White" />
    </VerticalStackLayout>
</ContentView>
